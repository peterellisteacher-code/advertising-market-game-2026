# Plan: tuckability + single-action redesign

Branch: `agent/tuckability-single-action-20260806`. Spec: `docs/superpowers/specs/2026-08-06-studio-tuckability-single-action-design.md`.

- **A1 — Tuck shell core (Studio top zone):** `tuck-shell.ts` primitive (TDD) + convert top bar and pair strip to default-tucked top-edge tabs; relocate undo/redo + save state to the canvas toolbar; keep tour/guided-journey working via untuck hooks; persistence, a11y, reduced motion; update affected tests.
- **A2 — Studio side zone + overlays:** active-tool-tab tucks drawer (remove "Hide tools"), drawer default per journey step, overlay exclusivity group (layers/inspector/section-fill/display).
- **B — First-entry pacing:** clean stage during tour, per-step auto-untuck via guided journey, staged reveal of edge tabs.
- **C — Godot surfaces:** lobby staging, HUD chip + tuck conformance, RunPanel audit. GodotIQ is wired locally (addon port 6105) but deliberately NOT shipped: the addon, its rule files and `godot/.godotiq.json` are gitignored, and `godot/project.godot` now carries three LOCAL-ONLY hunks (stock header comment rewrite from first editor open, `GodotIQRuntime` autoload, `[editor_plugins]` enable) — never stage that file as-is; hand-compose any real Phase C setting change without those hunks so the public tree (and the CI Linux export built from it) stays addon-free.
- **D — Task-loop finish:** writer's-statement evidence surface (mission + coach evidence roll-up), sound-booth decision (wire as optional audio step or retire), market screen conformance pass.
- **E — Release:** full gates, PR to main, CI artifact verification, draft Netlify QA (Peter enters the site password), production deploy + smoke at 1280x800/1440x900.

Each phase lands as its own reviewed commit series; suites stay green between phases.

## Session handover — 2026-08-06, end of Phase C

Worktree: `C:\Godot Projects\Advertising Market Game Worktrees\agency-clarity-tuckability` (branch `agent/tuckability-single-action-20260806` — work here, NOT the main checkout). State on origin:

- **Done and gate-verified:** A1 (`9fbf2549`), public-tree guard (`8475b1e5`), A2 (`317287bc`), B journey bar (`0019ee59`), tour/role-guide real-input hotfix (`aac2cc2d`), role-card slimming (`df7e410c`), **Phase C (`319e48ca`)** — lobby staged behind one primary action ("Practice on this computer" + always-visible pair-alias field; join-a-live-room column tucked behind a new "Join the class market" toggle mirroring the teacher-setup toggle pattern). AgencyHud and RunPanel were audited at runtime and are already conformant — zero changes. The commit's 165 deletions are verified-safe editor-save cleanup (13 orphaned StyleBoxFlat sub-resources from the old PitchTheatre extraction — each appeared exactly once in `319e48ca^` as its own declaration line, zero `SubResource(...)` references, which is what proves them orphans; plus redundant instance lines on 2 nodes: `type=` attrs and PitchTheatre anchor overrides equal to its base scene's root values). Gates at `319e48ca`: web 175 files / 2449 tests + tsc green; Godot headless suite green (`godot --headless --path godot --script res://tests/run_tests.gd`).
- **Production hotfix PR #24: still OPEN, awaiting Peter's release flow** (CI Linux artifact → draft Netlify QA, Peter types the password → merge → fail-closed production deploy). Branch `agent/tour-input-hotfix-20260806`, worktree `C:\Godot Projects\Advertising Market Game Worktrees\tour-input-hotfix`. This branch's own copy of the fix is `aac2cc2d`; the two implementations are pattern-identical — prefer this branch's version where they collide on merge.
- **Next: Phase D, web half first** (no bridge needed): (1) zod-OPTIONAL `missionEvidence` array (`{missionId, title, decisionId, effectText}`) on `CampaignDocumentV1` — old drafts stay valid, no migration; the spec designed this deliberately as the one narrow bridge-contract exception. (2) Writer's-statement view reachable from the Menu tuck + offered post-publish (audience brief, `strategy.aidaPlan`, `marketRoute.proofPoint`, mission `effectText` under rubric headings, accessible names of AIDA-linked canvas objects, print stylesheet + button) — student-facing copy through `/plain-language` → `/claude-scrubber` before ship. Then the **Godot half via `godot-builder`** (one-live-mutator): populate `missionEvidence` from `agency_progress.gd`'s `evidence_by_mission` on creator open/publish (`campaign_document.gd` validator passes it through), and PitchTheatre swaps generic checklist lines for the pair's own `effect` text where present. Field mapping (do not improvise): `decisionId` ← `evidence_by_mission[missionId].decision` (a choice **id**, see `agency_mission_controller.gd`'s `_selected_choice_id`), `effectText` ← `.effect` (the pair's 30–400 char sentence), `title` ← the mission's `title` in `agency_mission_catalog.gd`. Sound-booth is DECIDED in the spec: ambient set dressing, no mission wiring — that part of plan line D is closed. Plan line D's third item, the **market-screen conformance pass, is still OPEN**: audit-only (the spec's out-of-scope rules out a live-market redesign, not the audit) — fold it into the Godot half alongside PitchTheatre, RunPanel-style.
- **Bridge state:** proved out this session — `godotiq_editor_context` → `addon_connected: true` on addon port **6105** (`godot/.godotiq.json`, local-only). The editor was left RUNNING on this worktree's project (Main.tscn open) for Phase D's Godot half. If a fresh session finds it closed, relaunch: `& "C:\Users\Peter Ellis\Godot\godot_current.exe" --editor --path "C:\Godot Projects\Advertising Market Game Worktrees\agency-clarity-tuckability\godot"` and re-check `editor_context`; a connection REFUSED on 6105 means no editor/addon listening, while an MCP server dialling 6007 means that server process predates the port config and needs a Claude Code app restart. Do NOT re-run licence diagnosis or `godotiq auth reset` (receipt re-issued 2026-08-06, pro/active/38 tools).
- **Harness:** from the worktree root run `npx vite --port 4173` (matches the untracked `.claude/launch.json` "studio-harness" entry; the root `vite.config.ts` dev server serves the Studio). Fresh-session behaviour requires `sessionStorage.clear()` + reload (tuck state and tour acknowledgement are session-scoped by design).
- **Gates (worktree root):** `npx pnpm test` (= `node scripts/build-netlify-functions.mjs && vitest run --no-cache --configLoader runner --maxWorkers=1` — the build step is required on a clean tree because `netlify/deploy-layout.test.ts` asserts the gitignored bundles exist) and `npx tsc --noEmit`. **Phase D's contract change needs a third gate:** `npx pnpm run test:build-web` — vitest's include globs (`web/**`, `netlify/**`, `shared/**`) do NOT cover `scripts/`, and the Godot↔web parity assertion lives in `scripts/godot-bridge-contract.test.mjs` (reads `campaign_document.gd` against `web/src/domain/campaign-document.ts`), which only runs under that script. Godot-side changes also rerun `godot --headless --path godot --script res://tests/run_tests.gd`. The repo is **pnpm** (`pnpm-lock.yaml`, relative store in `.npmrc`): a fresh worktree needs `npx pnpm install --frozen-lockfile` (bare `pnpm` is not on this machine's PATH), never `npm ci`. Known flake under contention: `web/src/fabric/fabric-canvas-adapter.test.ts` (pre-existing, out of scope; passed this session's run).
- **Standing flags for Peter:** unapplied Supabase migration for teacher batch Image-Lab allowances (`docs/operations/image-lab.md:305-308`); `godot/project.godot` carries three LOCAL-ONLY hunks (stock header rewrite, `GodotIQRuntime` autoload, `[editor_plugins]` enable) — never stage it; a real Phase-C/D setting change must be hand-composed without them. Phase C's join-tuck flow was verified in the editor sandbox only — the web-export smoke lands with Phase E's draft Netlify QA.
