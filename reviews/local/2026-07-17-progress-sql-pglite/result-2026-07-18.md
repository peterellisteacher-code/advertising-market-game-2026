# PGlite SQL validation result — 2026-07-18

Command (working directory: `Codex Advertising Market Game`):

```powershell
node reviews/local/2026-07-17-progress-sql-pglite/validate-progress-sql.mjs
```

Result: `BLOCKED_BY_PGLITE_LIST_EXECUTION` (process exit `0`, because the
known blocker is asserted and reported by the harness).

The exact migration read by the harness had SHA-256
`894c8cad05e7ff13d3a343ec015e07e21dcb8cc039862777b274f6c8cdb1b602` and
executed in an in-memory PGlite 0.3.16 database after disposable bootstrap of
`anon`, `authenticated`, `service_role`, `auth.users`, and
`public.signal_lost`.

Evidence that passed: the dedicated role's seven capability flags were false
and it had no memberships; schema/table/function owners and the sole exact
seven-argument RPC matched; RLS was enabled with zero policies and no direct
schema/table access for all three API roles; only `service_role` could execute
the security-definer RPC; save/load/CAS conflict, missing/empty `teamId`
rejection, per-user load isolation, and the 16-document cap passed; the
unrelated sentinel stayed exactly `{ id: 7, payload: "must-remain-untouched" }`.

Blocking evidence: the first `list` RPC call reached the migration's exact
`pg_catalog.coalesce(jsonb, jsonb)` expression and PGlite returned
`function pg_catalog.coalesce(jsonb, jsonb) does not exist`. Consequently this
local run cannot establish successful metadata-only listing or list ordering,
although the cap was independently confirmed by 16 stored rows and a
`document_limit` 17th save. This is a local runtime finding, not a change to
the migration or a hosted-Supabase result.
