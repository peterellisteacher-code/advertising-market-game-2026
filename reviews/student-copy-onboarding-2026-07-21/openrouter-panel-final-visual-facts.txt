VISUAL AND VIEWPORT FACTS

- Target environment: Year 10 pairs using a recent school-issued MacBook, with Safari likely, at realistic laptop heights. The inspected viewports were 1366 x 768 and 1440 x 900 CSS pixels; mobile is outside this milestone.
- The direct OpenRouter multimodal request supplies five labelled JPEG contact sheets containing all 19 current screenshots. Each contact sheet is built only from the current PNG captures listed below; no historical screenshot is included. The textual and DOM facts below accompany the actual image bytes.
- The current source uses a cream, charcoal/navy, orange-red, pale-blue, and yellow visual system. The Godot shell has a thin orange top rule, bold sans-serif headings, a three-stage progress strip, rounded white content cards, and high-contrast primary buttons. The studio uses a dark navy command bar, a persistent pair/audience strip, a yellow tool-tab strip, and a three-part workspace: tool drawer, central canvas, and inspector.
- At 1366 x 768, a fresh practice pair completed the whole current rebuilt web-export flow from lobby through all three levels, market-card publication, backing two different sellers, Finish shopping, and the terminal Practice round complete state.
- Current 1366 x 768 captures cover: lobby; Level 1; Studio roles; Level 1 complete; Level 2; AIDA complete; Level 3; price complete; route start; route complete; Level 3 ready; final check; market gate; publication; market cards; and completion.
- At 1440 x 900, the fresh lobby, Level 1 run screen, and Level 1 studio were inspected. The Studio used a two-row pair strip; both role actions and Swap roles remained visible.
- Current DOM measurements at 1440 x 900 found document dimensions equal to viewport dimensions and no clipped visible button, input, select, or tab. At 1366 x 768, the role card had equal 56-pixel client and scroll heights. A below-fold Build control belongs to the intentionally scrollable tool drawer.
- The current run screen hides the long introductory hero paragraph. Before a level is ready it exposes Open Studio; readiness adds Lock Level; locking replaces those controls with Next level. The studio strip shows both the active and partner roles and actions, exact visible-change progress, and Swap roles. Audience detail remains behind Open full brief.
- The exact assembled build/web candidate was served unchanged with COOP/COEP headers. The QA server mocked only authenticated GET /api/account/session as qa-pair; it did not call or mutate Supabase, cloud progress, or production.
- Current console warning/error entries across the two final browser tabs: zero.
- Native Godot execution remained quarantined. The inspected browser candidate used a fresh isolated Godot web export whose PCK was assembled into the current build/web artifact.
