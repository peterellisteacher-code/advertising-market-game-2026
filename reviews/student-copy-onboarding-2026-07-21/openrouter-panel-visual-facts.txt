VISUAL AND VIEWPORT FACTS

- Target environment: Year 10 pairs using a recent school-issued MacBook, with Safari likely, at realistic laptop heights. The requested review viewports are 1366 × 768 and 1440 × 900 CSS pixels; mobile is outside this milestone.
- The OpenRouter `chat-send` transport used for every seat accepts text only. No image bytes are supplied to any seat, even where a model catalog says the model itself accepts images. Visual judgments must therefore be labelled as inferences from the textual visual evidence below.
- The current source uses a cream, charcoal/navy, orange-red, pale-blue, and yellow visual system. The Godot shell has a thin orange top rule, bold sans-serif headings, a three-stage progress strip, rounded white content cards, and high-contrast primary buttons. The studio uses a dark navy command bar, a persistent pair/audience strip, a yellow tool-tab strip, and a three-part workspace: tool drawer, central canvas, and inspector.
- Current source hides the long introductory hero paragraph. Before a level is ready, the run screen exposes Open Studio; readiness adds Lock Level; locking replaces those with Next Level. The current studio source shows the active role and its immediate action in the persistent strip; the partner action and audience detail remain behind Open full brief. The strip's CSS minimum height is 76 pixels and its role text may wrap instead of ellipsising.
- Historical baseline captures are available as 1920 × 914 PNG files from 17 July 2026. They predate the current progressive-control and pair-strip changes and are not attached to this text-only transport. Their visible layouts were:
  - Lobby: brand and one-line game premise above a rounded card; join-room and host-market forms sit in two columns; Practice on this computer sits in a separate bottom row; status copy sits below the card.
  - Level 1: three-stage strip across the card; level heading and clue; historical Open, Lock, and disabled Next controls in one row; status below. Current source changes the visibility sequence described above.
  - Studio: dark top command row; a wide role/audience instruction area; yellow tool tabs; product controls at left, white canvas in the centre, inspector at right. The historical strip showed both role instructions; current source uses the active action plus an expandable full brief.
  - Market gate: three-stage strip, a heading and one Build the market card action in the upper part of the main white card, with status below.
  - Market: three-stage strip, a Live Market heading and spending rule in the upper part of the main white card, with status below.
- A fresh current capture could not be completed: the local candidate server returned HTTP 200, then the in-app browser blocked navigation from its retained `data:` error tab under Browser Use URL policy. This is a browser-tool limitation, not evidence that the game failed to load. No hosted, Safari, overflow, or clipping claim is established by that attempt.
- The TypeScript studio candidate was rebuilt successfully. Native Godot execution and export remain quarantined on this Windows/OneDrive machine, so the current GDScript/TSCN navigation changes are source-and-test candidate changes rather than a newly exported binary screenshot.
